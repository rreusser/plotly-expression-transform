'use strict';

var assert = require('chai').assert;
var plotlyTransformExperiments = require('../');

describe('plotly-transform-experiments', function () {
  it('passes a test', function () {
    assert(plotlyTransformExperiments());
  });
});
